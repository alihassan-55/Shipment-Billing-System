{
  "name": "courier-billing-system",
  "version": "1.0.0",
  "private": true,
  "description": "Courier Billing System - Integrated Server and Client",
  "scripts": {
    "install:all": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "deploy:build": "cd client && npm install && npm run build && cd ../server && npm install",
    "prisma:generate": "cd server && npm run prisma:generate",
    "prisma:migrate": "cd server && npm run prisma:migrate"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "courier",
    "billing",
    "express",
    "react"
  ]
}


